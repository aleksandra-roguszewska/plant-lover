import styled from "styled-components";

export const StyledPasswordInputCotainer = styled.div`
  display: flex;
  position: relative;
  width: 100%;

  button {
    border: none;
    background-color: transparent;
    position: absolute;
    top: 16px;
    right: 16px;
  }

  img {
    height: 3rem;
    width: 1rem;
  }
`;
